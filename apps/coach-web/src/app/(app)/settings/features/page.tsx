import SettingsPlaceholder from '@/components/settings/SettingsPlaceholder';

export default function Page() {
  return (
    <SettingsPlaceholder
      title="Features"
      description="Feature entitlements are plan-gated via Command Center — academy view coming next."
    />
  );
}
