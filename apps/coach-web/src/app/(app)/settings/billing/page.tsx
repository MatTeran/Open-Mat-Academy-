import SettingsPlaceholder from '@/components/settings/SettingsPlaceholder';

export default function Page() {
  return (
    <SettingsPlaceholder
      title="Billing"
      description="Subscription and plan self-service will surface entitlements from Command Center plans."
    />
  );
}
